// DEPRECATED — eliminado en el refactor de sesión cookie (2026-06-30).
// Reemplazado por useSessionBootstrap + cookieSession.
// Mantenido como no-op para que las importaciones existentes no rompan durante la migración.
// TODO: eliminar este archivo y todos sus imports una vez confirmado el refactor.

/** @deprecated Usar useSessionBootstrap en su lugar. */
export function useMockSessionFromUrl() {}
