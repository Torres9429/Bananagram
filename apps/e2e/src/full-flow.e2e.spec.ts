describe('Full Flow E2E', () => {
  it('registro → login → crear marca → crear post → aprobar → publicar → reporte', async () => {
    // TODO: implementar con supertest contra el API Gateway
    expect(true).toBe(true);
  });
});
